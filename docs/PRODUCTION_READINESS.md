# Production Readiness Tracker

Living record of the 2026-08-21 production-readiness and security audit, updated as controls are implemented or re-verified. This file is the source of truth — AGENTS.md §34 states the same thing: a written rule is not proof a control exists.

**Statuses used:** `Implemented` · `Partially Implemented` · `Not Implemented` · `Not Applicable Yet` · `Cannot Verify`

**How to read "Verification status":** who checked this, when, and how — not just whether it currently looks true. A row is only `Implemented` when its evidence was actually re-read/re-run at the date shown, not recalled from a prior audit.

---

## How this tracker stays accurate

- **Every PR** that touches an endpoint, table, or workflow listed below updates the corresponding row in the same PR — not a follow-up.
- **Every CI run** exercises whatever is wired into `npm test` (see CI-01/TEST-01 below) — a green CI run is evidence only for what actually ran.
- **Before every deployment**, re-check any row whose evidence lives outside this repo (Supabase dashboard settings, GitHub branch protection) — CI cannot verify those.
- **Monthly, post-launch**, re-verify every `Cannot Verify` row against the live project, and spot-check a few `Implemented` rows against current code to catch silent regressions.
- A control may only be marked `Implemented` when the evidence cited was read or executed at the "Last verified" date — not inferred from this document's own history.

---

## Phase 1 — completed 2026-08-21 (this change)

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| `npm test` runs unit + workflow + DB suites in one deterministic command | **Implemented** | Low | Before starting Automation 2 | `package.json`: `"test": "npm run test:unit && npm run test:workflow && npm run test:db"`; `test:unit`/`test:workflow`/`test:db` each runnable independently. **This total counts only what the Node test runner (`node --test`) actually executes** — Playwright browser scenarios are tracked separately below and are never folded into this number | None — keep new suites wired into all three as they're added | Verified 2026-08-24 (auth-UX/post-auth-routing phase, on top of the 2026-08-22 rate-limiting work; password minimum corrected from 12 back to 8 same day — see the dedicated row below): `npm test` executed locally, **483/483 Node tests passing** — 176 unit (`test:unit`, 38 suites, up from 128/32), 103 workflow (`test:workflow`, unchanged), 204 db (`test:db`, 42 suites, up from 187/34). New this phase: `tests/unit/auth-email-validation.test.mjs` (18, new — `src/lib/authValidation/email.ts` normalize/format-validate coverage, including the exact reported plus-address example `batoul+test@gmail.com`), `tests/unit/auth-password-policy.test.mjs` (11, new — the shared 8-char minimum, 64+-char/Unicode/space acceptance, and a source-level regression check that no auth surface redeclares its own `MIN_PASSWORD_LENGTH`), `tests/unit/onboarding-step-to-path.test.mjs` (8, new — the single shared `next_step` → path mapping, `src/lib/entitlements/onboardingStep.ts`), `tests/db/onboarding-routing-matrix.test.mjs` (14, new — the full State A–E post-auth routing matrix against the real `get_onboarding_readiness()` RPC, including no-duplication-on-repeated-sign-in), `tests/db/auth-credential-policy.test.mjs` (6, new — real-GoTrue password-length enforcement at the 8-char minimum, no-truncation at 64+ chars, email-casing consistency, and a plus-addressed signup/sign-in round trip). Re-ran the full `test:db` suite 3 times and the concurrency-heavy `rate-limits`/`auth-rate-limit`/`payment-attempt-rate-limit` files 3 additional times in isolation while investigating a previously-reported flaky DB test — all 6 runs were clean; the specific flaky test could not be reproduced and so was not "fixed" with an unverified guess (see the dedicated row below) |
| CI runs unit and workflow tests | **Implemented** | Low | Before starting Automation 2 | `.github/workflows/ci.yml`: `Unit tests` (`npm run test:unit`) and `Workflow tests` (`npm run test:workflow`) steps, before `Build` | None | Verified 2026-08-21: YAML validated with `js-yaml`; steps run the same scripts confirmed locally |
| CI runs DB integration tests (`tests/db`) | **Not Implemented** | Medium | Before starting Automation 2 | `.github/workflows/ci.yml` has a comment explaining the exclusion; no `test:db` step exists | See "DB tests in CI" section below — requires a validated `supabase/setup-cli` + `supabase start` job, proven with a manual/canary run before it gates merges | Documented 2026-08-21 — not yet attempted against a real GitHub-hosted runner |
| AGENTS.md guardrails for tests, uploads, auth/API surfaces, account deletion, n8n, and secrets-in-output | **Implemented** | Low | — | `AGENTS.md` §34 (added 2026-08-21) | Keep §34 in sync whenever a referenced control's status changes here | Verified 2026-08-21: section read back after edit |

### DB tests in CI — investigation result

**Decision: left out of CI in this phase.** The five safety conditions given for this task can be satisfied in principle, but the sixth — *"the configuration is reliable on GitHub-hosted runners"* — cannot be proven from this environment:

- Running `tests/db` requires the **full local Supabase stack** (Postgres, GoTrue, PostgREST, Storage — this repo's tests exercise real RLS through real per-user sessions, not mocks; see `tests/db/helpers.mjs`). Starting that stack means running `supabase start`, which pulls and runs several Docker containers.
- GitHub-hosted `ubuntu-latest` runners have Docker available, and the official `supabase/setup-cli` action plus `supabase start` is a documented, commonly-used pattern — but its reliability depends on image-pull latency, the standard runner's 2-core/7GB resource ceiling, and known community reports of intermittent slowness/flakiness from heavier auxiliary services (analytics/logflare, edge-runtime, imgproxy) that this project's tests never actually need.
- Proving reliability requires an actual run against a GitHub-hosted runner — which this controlled, no-push task cannot do (pushing/triggering Actions is out of scope until you approve it). Per your instruction not to improvise past that point, DB tests stay out of CI until reliability is demonstrated with a real run.

**Safest path forward (Phase 2+, not implemented now):**
1. Add a **separate**, initially **non-required** CI job (`db-tests`), so a flaky run never blocks merges while it's being proven out.
2. Use `supabase/setup-cli@v1` pinned to a specific version, then `supabase start` (excluding services this project's tests don't use, if the installed CLI version supports selective startup — e.g. `--exclude analytics,imgproxy,edge-runtime`) to keep the stack minimal and faster to boot.
3. Read connection details from `supabase status -o env` **within the job** (never committed) and export them as job-scoped env vars for `npm run test:db` — no secret is ever stored in the repo or in GitHub Secrets, since these are ephemeral local-only credentials generated fresh per run.
4. Run `npm run test:db`, then `supabase stop` (the runner's teardown would also discard everything regardless).
5. Watch it run green on several real PRs before promoting `db-tests` to a required check.

---

## Full findings (from the 2026-08-21 audit) — unchanged this phase

Everything below is carried forward from the audit as-is; Phase 1 intentionally did not touch rate limiting, security headers, auth behavior, migrations, RLS policies, or n8n. Timing labels that previously read "before merging this auth branch" have been updated to **"before starting Automation 2"**, since the auth branch this label referred to is the one being finished in this very change.

### Security

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| RLS enabled and narrowly scoped per table | Implemented | Low | — | `supabase/migrations/*.sql` (cvs, profiles, matches, applications, jobs, notifications, audit_events, automation_tasks, analysis_feedback) | Re-check the live project once RLS is verifiable (see DB-01) | Verified 2026-08-21 via migration source only |
| App-level rate limiting on abuse-sensitive routes | **Implemented for every surface with meaningful abuse/cost impact** | High → Low | — | Corrected 2026-08-22 (this row was stale — it predated the auth-surface work below it in this same table). Covered: all four pre-auth surfaces (login/signup/forgot-password/oauth-init), `GET /auth/callback` (session-keyed), password update after recovery (user-keyed), CV upload/replace, AI-analysis-task creation, feedback submission, and checkout/payment-attempt creation. Deliberately **not** limiter-gated, by design, with the protection each inherits instead: preference saves and onboarding-completion (the raw save must stay flexible — AI-task creation is inherited via `create_analysis_task`'s dedup + shared quota); `cv-analysis/confirm` and `cv-analysis/review` (cheap, idempotent, ownership-checked DB writes on already-existing rows, no AI/external cost — abuse here has no meaningful resource/security impact beyond ordinary DB load, so a limiter would be an arbitrary blanket cooldown, not a real control). See the "Rate limiting and abuse prevention" table below for the full per-surface breakdown | None planned for the covered surfaces. Full IP/network-layer limiting (CDN/reverse-proxy) remains a "later at scale" hardening, not required for the surfaces above, which are already identifier-hash- or user-keyed | Verified 2026-08-22: `npm run test:db` (187/187), full manual re-read of every route/page listed, and live Playwright verification (see the "Playwright E2E verification" section under Testing) |
| Security headers (CSP/HSTS/etc.) | Not Implemented | High | Before deployment | `next.config.ts` has no `headers()` function | Add a `headers()` block (Phase 2 — explicitly out of scope for Phase 1) | Verified 2026-08-21 |
| Password policy consistency (client vs. Supabase Auth) | **Implemented (local)** | Medium | Before deployment | Consistent at 8 chars everywhere via one shared module, `src/lib/authValidation/password.ts` (`MIN_PASSWORD_LENGTH = 8`, `MAX_PASSWORD_LENGTH = 200`), now imported by `signup`/`login`/`update-password` routes and the `signup`/`reset-password` pages — previously four independent `const MIN_PASSWORD_LENGTH = 8` declarations that could drift from each other even though they already agreed on the same value. **Correction 2026-08-24**: this row briefly read "raised to 12" earlier the same day; the minimum was reverted to 8 per explicit instruction (12 was not the intended policy) — `supabase/config.toml`'s `minimum_password_length = 8` (was 6, momentarily 12, now 8) matches. No composition rules on either side (`password_requirements = ""`), so the two layers cannot disagree by construction. Long passwords, spaces, Unicode, paste, and password-manager-generated values all remain accepted and are never truncated (`MAX_PASSWORD_LENGTH = 200`). Production dashboard value is still separate and unverified | Set the hosted project's dashboard minimum to 8 to match before deployment | Verified 2026-08-24 (twice — once at 12, corrected and re-verified at 8): local Supabase stack restarted after the config change; `tests/db/auth-credential-policy.test.mjs` proves GoTrue itself (not just the app) rejects a 7-char password and accepts exactly 8, and that a 69-char password is accepted and never silently truncated (a truncated prefix fails to sign in, the full string succeeds); `tests/unit/auth-password-policy.test.mjs` proves signup and reset-password import the identical shared constant (no per-surface drift) |
| Password field show/hide controls (signup, login, reset-password) | **Implemented** | Low | — | New shared `src/components/auth/PasswordField.tsx` (self-contained `useState` toggle, real `type="button"` so it can never submit the form, native keyboard operability, `aria-label` that flips between "Show/Hide {label}") replaces the plain `type="password"` `FormField` on `signup`/`login`, and replaces `reset-password/page.tsx`'s previously-duplicated local copy of the same component | None | Verified 2026-08-24: manual code review of all three call sites; `npm run build` compiles all three pages; visual/keyboard behavior confirmed live via Playwright (see the "Playwright E2E verification" section under Testing) |
| Email trim/lowercase/format-validation consistency (signup, login, forgot-password) | **Implemented** | Medium | Before deployment | New shared `src/lib/authValidation/email.ts` (`normalizeEmail` — trim + lowercase, never Gmail-style dot/plus canonicalization; `isValidEmailFormat` — rejects missing local-part/domain/`@`/embedded whitespace, accepts any domain and `+tag` addressing) now used by all three client pages before submit and all three API routes (`login`, `signup`, `forgot-password`) before normalizing/validating and before calling Supabase Auth — previously each surface only `.trim()`ed and never validated format or case at all, so `Test@Example.COM` and `test@example.com` could in principle be treated as different submissions by the app layer (rate-limit hashing) even though Supabase Auth itself resolves them to the same account. Plus-addressing (e.g. `batoul+test@gmail.com`) is explicitly preserved end-to-end — the full normalized address, `+tag` included, is treated as the submitted identity, never split or canonicalized | None | Verified 2026-08-24: `tests/unit/auth-email-validation.test.mjs` (18/18 — trim, lowercase, plus-addressing preserved including the exact reported example `batoul+test@gmail.com`, dot-in-local-part preserved, malformed/missing-domain/missing-local-part/embedded-whitespace rejection, multi-`@` rejection, length boundary); `tests/db/auth-credential-policy.test.mjs`'s casing test confirms Supabase Auth itself already resolves a mixed-case signup and a lowercase sign-in to the same `auth.users.id`, and its plus-addressing test proves a real `+tag` signup/sign-in round trip against local GoTrue preserves the address exactly, so the app's own normalization cannot introduce a mismatch with Supabase's behavior |
| `profiles.role` not client-writable | Implemented | Low | — | `20260809090020_add_profiles_role.sql` — column-level GRANT excludes `role` | None | Verified 2026-08-21 |
| Live-project RLS matches migration source | Cannot Verify | Medium | Before deployment | Requires production Supabase dashboard access | Confirm via Supabase dashboard → Auth → Policies before launch | Not verifiable from this environment |
| Post-login/signup onboarding routing — single authoritative resolver | **Implemented (fixed)** | High → Low | — | **Root cause investigation** (2026-08-24): the reported bug ("an existing user with a CV/preferences/analysis signs in — especially via Google — and lands on `/onboarding/upload-cv`") was audited from two angles. (1) **Identity linking**: confirmed via Supabase's own documentation (`supabase.com/docs/guides/auth/auth-identity-linking`) that automatic OAuth identity linking is Supabase Auth's default, non-configurable behavior — a new Google sign-in whose email matches an existing *verified* email resolves to the *same* `auth.users.id`, not a new one (this local project auto-confirms email/password signups, so every existing account here qualifies). `enable_manual_linking` in `supabase/config.toml` is unrelated — that flag only gates the separate `linkIdentity()` API for adding a provider to an already-authenticated session, not this automatic-on-sign-in linking. Given this, a genuinely separate Google identity is not expected in this app's current configuration, and `handle_new_user`'s `after insert on auth.users` trigger (which would otherwise create a second `profiles` row) only fires on an actual `INSERT`, which automatic linking does not perform. **This is documented, not yet re-confirmed against a real Google account** — Playwright cannot drive Google's own login UI, so final confirmation is a manual-takeover step in a future Playwright pass (see the "Playwright E2E verification" section under Testing); if a real test ever shows two distinct `auth.users.id` rows for the same person, that would be a genuine identity-linking gap requiring its own fix, distinct from the routing bug below. (2) **The actual routing bug**: `src/app/auth/callback/route.ts` (Google OAuth) already computed its destination from `get_onboarding_readiness()` correctly, but `src/app/api/auth/login/route.ts` (email/password) never consulted readiness at all — every successful login unconditionally returned `"/dashboard"`, regardless of onboarding state. This is the reverse-direction latent bug (a genuinely CV-less returning user bypassing onboarding), and it also meant the two auth methods could never share one routing decision by construction, which is what the task required. **Fix**: extracted the destination logic (previously inlined only in the callback route) into `src/lib/entitlements/postAuthDestination.ts` (server-only `resolvePostAuthDestination()`, wrapping the existing, already-correct `get_onboarding_readiness()`) plus a zero-dependency `src/lib/entitlements/onboardingStep.ts` (`onboardingStepToPath`, pure, directly unit-tested). `login/route.ts` now calls the same resolver after a successful `signInWithPassword` and returns `{ ok: true, destination }`; `login/page.tsx` uses that `destination` (an explicit safe `next` still takes precedence — same precedence as the callback route). `src/lib/supabase/session.ts` (the proxy's already-authenticated-visits-`/login`-or-`/signup` redirect) was also updated to call the same RPC and the same pure mapping function instead of unconditionally redirecting to `/dashboard`, so a browser-back-button visit to `/login` after a fresh sign-in is routed by the same rules too. Every call site now derives its decision **only** from `get_onboarding_readiness()`, which itself evaluates `auth.uid()` from the caller's own server-verified session — never a client-supplied flag, query param, or provider redirect | None planned for the routing fix itself. The identity-linking finding should be re-confirmed with a real Google account during the next Playwright session that has interactive Google access, and documented as closed (or escalated) at that point | Verified 2026-08-24: `tests/db/onboarding-routing-matrix.test.mjs` (14/14) proves `get_onboarding_readiness()` returns the correct `next_step` for States A (no CV) through E (repeated sign-in, asserting zero row-count change across `profiles`/`cvs`/`job_preferences`/`subscriptions` after 5 repeated readiness reads and 3 repeated admin sign-in cycles), including every `cv_profile` analysis sub-state (pending/processing/completed/failed/ownership-mismatch/changes-requested) staying on `/dashboard`; `tests/unit/onboarding-step-to-path.test.mjs` (8/8) proves the pure mapping function itself; `npx tsc --noEmit` and `npm run build` both pass with the refactor; live Playwright verification of both login and Google OAuth landing pages not yet run for this branch's final state (see the "Playwright E2E verification" section under Testing) |

### File handling

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Client/Storage-bucket/RPC MIME + size checks on CV upload | Implemented | Low | — | `onboarding/upload-cv/page.tsx`; `20260811090000_restrict_cv_to_pdf.sql` | None | Verified 2026-08-21 |
| Server-side byte-level (magic-number) file validation | Not Implemented | Medium | Soon after launch | Only MIME-type/extension checks found at every layer | Add a PDF header/magic-number check (AGENTS.md §34 now requires this eventually) | Verified 2026-08-21 |
| CV upload/replace error messages never expose raw Postgres/Storage text | **Implemented** | Low | — | Fixed in an earlier session (see git history); this session's re-audit (2026-08-22, "Do not assume the previous report is still accurate") independently re-read `upload-cv/page.tsx` in full and confirmed the fix is still in place, unmodified: `storageError` always maps to a generic message; `dbError` is mapped by known cause — `PT429` (rate limit), `23514`/"storage_path must be scoped" (validation/ownership), `"Not authenticated"` (redirects to `/login`), else a generic message — `.message` is never read for display in any branch. The same review additionally re-read every other modified route/page this phase (login, signup, forgot-password, reset-password, preferences, checkout, submit-feedback, profile update-name) and confirmed the same allowlist-of-known-safe-strings pattern holds everywhere — no new leakage found, no code change was needed for this specific finding | None planned | Verified 2026-08-22: manual code review of every branch in every modified auth/profile/feedback/checkout route confirms no raw error text reaches the UI; exercised live via Playwright (see the "Playwright E2E verification" section under Testing) |

### API protection

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Explicit allowlisted fields on every write | Implemented | Low | — | `parseBody`/`parseRequestBody` guards in every `src/app/api/**/route.ts` | None | Verified 2026-08-21 |
| Minimal API responses (no unnecessary `select("*")`) | Partially Implemented | Low | Soon after launch | `cv_analyses` reads use `select("*")` in `src/lib/cvAnalysis/review.ts` and the dashboard, shipping `extracted_text` to the client | Select only the columns each view actually renders | Verified 2026-08-21 |

### Rate limiting and abuse prevention

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Auth endpoints (login/signup/OAuth-init/OAuth-callback/forgot-password/password-update) | **Implemented (all six surfaces)** | Medium | Before deployment | **Root cause** (unchanged from the login-only phase): `supabase/config.toml`'s `sign_in_sign_ups = 30` (5 min/IP, shared by login and signup) is never actually enforced locally — confirmed by inspecting the running GoTrue container's environment (`docker exec supabase_auth_<project> env`): every other `[auth.rate_limit]` key is correctly translated into a `GOTRUE_RATE_LIMIT_*` env var by the installed Supabase CLI (2.109.1), but `sign_in_sign_ups` is not. Reproduced empirically for login: 100 invalid + 15 valid password-grant requests, zero 429s. **Coverage now complete for all six auth surfaces**, each DB-authoritative and identifier-hash-keyed (never IP, never a raw email/password/OAuth token/reset link — see `supabase/migrations/20260822130000_generalize_auth_rate_limits.sql`): login (email, 5/15min), signup (email 5/15min **+** an opaque per-browser session cookie 8/15min so rotating the submitted email cannot bypass the limit), forgot-password (email 3/15min **+** session 5/15min, so mail-bombing many different real addresses from one browser is also bounded), Google OAuth initiation (session only, 10/15min — there is no email at initiation time), Google OAuth callback / password-reset-link verification (session only, 20/15min — generous specifically so a real one-click provider redirect or reset-link click is never affected; `src/app/auth/callback/route.ts`), and password update after a recovery sign-in (user-id-keyed, 8/15min — by this point the caller has a verified authenticated session, so their own id is the correct identifier, not email or the anonymous session cookie; `src/app/api/auth/update-password/route.ts`, added 2026-08-22 to replace the previous direct client-side `supabase.auth.updateUser()` call in `reset-password/page.tsx`, which had no rate limiting at all). All six are called only from server-only Next.js code via `createAdminClient()` (service-role) — see `supabase/migrations/20260822150000_add_oauth_callback_and_password_update_rate_limits.sql` for the two newest actions/kinds and `src/lib/authRateLimit/rateLimit.ts` for every policy value in one place. **Vulnerability found and fixed during this phase**: the login-only revision granted `EXECUTE` on the rate-limit RPCs to `anon`/`authenticated`, so any unauthenticated caller could compute a victim's email hash themselves and call the RPC directly, repeatedly, to pre-exhaust that one victim's login quota — a targeted DoS bypassing the login route entirely. Fixed by revoking `anon`/`authenticated` and granting `service_role` only; proven via a dedicated regression test that anon calls are now rejected. **Bug found and fixed via Playwright, not caught by DB-level tests**: the signup route originally cleared the session-keyed gate on every successful signup, which reset the very counter meant to stop email rotation — an attacker could create unlimited accounts since each success zeroed the counter again. Fixed: only the email-keyed gate is cleared on success; the session-keyed gate is never cleared (see that route's own comment) | Production dashboard values for `sign_in_sign_ups` remain unverified — confirm before launch, and re-run the same empirical container-env check against whatever hosts the production GoTrue instance, since the same CLI-version gap could apply there too. **Signup duplicate-email handling corrected 2026-08-22** (see the dedicated row below): the route previously returned a raw HTTP 500 for a duplicate signup against an already-confirmed account (the normal local-dev case) because it only special-cased Supabase's other, unconfirmed-user duplicate path (a 200 with an empty `identities` array). Now both duplicate paths map to one identical, deliberately-neutral 409 response — signup is no longer a full "intentional exception" to enumeration resistance, though a residual signal remains: a genuinely new signup still visibly succeeds (or asks for email confirmation), so a duplicate email is still distinguishable from a brand-new one by response content alone, just not by a raw error or an affirmative "this account exists" statement. Full response-level indistinguishability would require an email-only notification flow for existing-email signups, which is out of scope for this fix | Verified 2026-08-22: `tests/db/auth-rate-limit.test.mjs` (17/17, up from 13 — 4 new tests cover the oauth_callback/password_update actions and the new 'user' identifier_kind) — N-then-blocked with positive `retry_after_seconds`, clear-on-success resets, per-identifier/per-action/per-identifier_kind scoping, reset after the window (backdated), the email-rotation-cannot-bypass-the-session-gate scenario tested directly at the RPC level, malformed/invalid-parameter rejection, concurrency (8 concurrent → exactly the configured limit succeed), and the anon-cannot-call-these-functions-at-all regression (proving the vulnerability fix, now re-verified for the two new actions too). **Fail-closed policy** (see `src/lib/authRateLimit/rateLimit.ts`'s `reserveAuthAttempt` — its comment previously and incorrectly said "fail-open"; corrected 2026-08-22, no behavior change, since every one of these six call sites was already returning a safe 500/redirect and never proceeding on a `null` result): if the limiter itself cannot be verified, the request is rejected, not allowed through — proven by `tests/unit/auth-rate-limit-fail-closed.test.mjs` (7/7). All six surfaces additionally reproduced live via Playwright against the real endpoints, including the signup bug found and its fix confirmed (see the "Playwright E2E verification" section under Testing) |
| Latest-state scheduling/deduplication (`create_analysis_task`) | **Implemented** | Low | — | `supabase/migrations/20260821090000_add_ai_task_and_cv_replace_rate_limits.sql`, corrected 2026-08-21 (twice) — first replacing an initial blanket-10-minute-cooldown revision that had a lifecycle bug, then fixing a feedback-linkage gap and adding real sequential abuse protection (see the next two rows — these are three *distinct* concerns, kept separate deliberately). Enforced inside the `SECURITY DEFINER` function itself (not a Next.js route), so it cannot be bypassed by calling `submit_analysis_feedback`, `update_profile_name_and_retry_analysis`, `save_job_preferences`, or `create_analysis_task` directly via `supabase.rpc()`. Design: dedup first (an active pending/processing task for a cv_id is never duplicated, and is never itself quota-charged); a pending task is collapsed in place (its `trigger` refreshed) when a different legitimate trigger arrives — the worker reads CV/preferences live at claim time, confirmed from `n8n-workflows/cv-analysis-worker.ts`, so no data is ever stale; a task already `processing` is flagged (`needs_followup`/`followup_trigger` columns) instead of duplicated, and an `AFTER UPDATE` trigger (`enqueue_followup_analysis_task`) automatically queues exactly one follow-up the moment it leaves `processing`, guarded against a since-replaced or since-superseded CV | Phase 2: also add a Next.js-route fast-path check ahead of the DB call, purely as a latency/cost optimization — the DB layer is already the authoritative gate | Verified 2026-08-21: `tests/db/rate-limits.test.mjs` sections A–C — collapse-in-place for a differing trigger while pending, flag-and-defer while processing (completion and failure paths), no follow-up for a replaced or superseded CV, per-user scoping, direct-RPC-call rejection, id-spoofing rejection, concurrency (5 concurrent callers on one CV collapse to exactly one task), and the full CV-replacement lifecycle regression, all passing |
| Feedback linkage across a deferred follow-up (`submit_analysis_feedback`, `enqueue_followup_analysis_task`) | **Implemented** | Low | — | Same migration. Risk found in review: `analysis_feedback.analysis_task_id` pointed at a specific task, but a deferred follow-up gets a brand-new id, and the n8n worker's "Load Task Feedback" step (`analysis_feedback?analysis_task_id=eq.{taskId}&limit=1`, no `order=` — read from `n8n-workflows/cv-analysis-worker.ts`/`.json`, not modified) filters by whatever task it just claimed. Fixed entirely in Postgres: `analysis_feedback` gained a `superseded_at` column, and the invariant "at most one feedback row is ever live (non-null `analysis_task_id`, `superseded_at` null) per task" is maintained by `submit_analysis_feedback` (superseding — detaching, not deleting — any earlier live row for the same task before inserting the new one, so the newest feedback is authoritative) and carried forward by `enqueue_followup_analysis_task` (re-pointing that one live row, if any, from the terminal task to the new follow-up). Because at most one row can ever match a live `analysis_task_id`, the worker's existing unordered `limit=1` read is deterministic with **no n8n change** | None planned — full history is preserved (rows are detached, never deleted) | Verified 2026-08-21: `tests/db/rate-limits.test.mjs` section D (4/4) — feedback submitted while processing is moved to the follow-up and found there by a query that exactly mirrors the worker's own; 3 rapid submissions while processing resolve deterministically to "newest wins," with all 3 preserved as permanent history; feedback survives both completion and failure of the flagged task; feedback that never needed to move stays exactly where it was |
| Feedback/user-request quota (`charge_feedback_task_quota`) | **Implemented** | Low | — | Same migration. Latest-state scheduling prevents *simultaneous* duplicate tasks but did not bound *sequential* ones — a user could complete one feedback-driven task and immediately request another, indefinitely. Added: 5 successful new requests per rolling hour per user, shared across `cv_correction`, `recommendation_feedback`, and `user_request` (including `update_profile_name_and_retry_analysis`'s own `user_request` trigger) — one bucket, not three, since all four represent the same cost/abuse profile and splitting them would let a user get 20 free re-analyses/hour by rotating which one they use. Charged only when a genuinely new request is *accepted* (a fresh task, or the first flag on a processing task) — never on a dedup/collapse, never on a failed/rejected call. Never applied to `cv_replaced` or `preferences_updated` — those must always be able to schedule the latest state, bounded instead by `replace_cv`'s own quota and `save_job_preferences`' own change-detection. `update_profile_name_and_retry_analysis` catches an exhausted-quota rejection internally so the name change itself still succeeds (`has_active_task: false`) — deliberately different from `replace_cv`, which never catches its own rate-limit error, because renaming has value independent of the retry, unlike a CV replacement's task. `POST /api/cv-analysis/submit-feedback` maps `PT429` to a friendly 429 response (`src/app/api/cv-analysis/submit-feedback/route.ts`); the existing dashboard error-surfacing path (`CvProfileSection.tsx`) already displays it generically, no raw Postgres text | None planned | Verified 2026-08-21: `tests/db/rate-limits.test.mjs` section E (10/10) — 5 succeed/6th rejected, dedup/collapse never charged, a failed validation never charged, the quota never blocks `cv_replaced` or `preferences_updated` even when fully exhausted, `update_profile_name_and_retry_analysis` degrades gracefully and shares the same bucket as feedback, per-user scoping, and concurrency (7 concurrent independent requests → exactly 5 succeed) |
| CV-replacement task-creation lifecycle bug (found and fixed during Phase 1) | N/A — historical note | — | — | An earlier revision of the migration above caught and silently swallowed `create_analysis_task`'s rate-limit error inside `replace_cv`, so a successful CV replacement could end up with no analysis task at all whenever some other trigger had recently created one. Root-caused to the blanket cooldown itself (see the "Latest-state scheduling" row above) and fixed by replacing it with version/state-aware scheduling, not by re-adding a catch — `replace_cv` calls `create_analysis_task` unconditionally, with no try/catch around it | — | Verified 2026-08-21: `tests/db/rate-limits.test.mjs`'s "CV replacement always results in a scheduled analysis" suite exercises this directly |
| CV-replacement quota (`replace_cv`) | **Implemented** | Low | — | Same migration: at most 5 successful replacements per user per rolling hour, enforced with `auth.uid()` inside `replace_cv` — the only write path for `cvs` already, so there is no separate app-layer gate to bypass. A rejected/failed call never consumes quota. Distinct from both the latest-state scheduling row and the feedback/user-request quota row above — this is a separate, independent quota keyed on the CV-replacement action itself, not on AI-task creation | Optional hardening (not required for the core goal): a `storage.objects` trigger on the `cvs` bucket to also bound raw Storage uploads that never call `replace_cv` at all | Verified 2026-08-21: `tests/db/rate-limits.test.mjs` — 5 succeed/6th rejected, failed-attempt-does-not-consume-quota, post-window success, per-user scoping, and concurrency (7 concurrent callers → exactly 5 succeed) all passing |
| App routes: rate limiting on feedback, profile-name retry, and checkout | **Implemented** | Low | — | `submit-feedback`/`update-name` inherit `create_analysis_task`'s shared feedback quota (5/1hr); `POST /api/checkout` gained its own quota this session — `create_payment_attempt()` now bounds genuinely new payment-attempt creation to 10/rolling-hour per user, charged only when a fresh row is inserted, never on a reuse of an in-flight attempt (`supabase/migrations/20260822160000_add_payment_attempt_create_rate_limit.sql`; `CheckoutRateLimitedError` in `src/lib/payments/checkout.ts` maps `PT429` to a safe 429, never raw RPC text) | None planned | Verified 2026-08-22: `tests/db/payment-attempt-rate-limit.test.mjs` (5/5) — idempotent reuse never charges the quota, 10 new attempts succeed/11th rejected, a terminal attempt is never reused, per-user scoping, concurrency (5 concurrent calls for one in-flight attempt resolve to exactly one row) |
| App routes: onboarding-complete, preferences, cv-analysis confirm/review — no new limiter (by design) | Not Applicable | — | — | Considered and deliberately left as-is this session. `onboarding/complete` and `save_job_preferences` must stay flexible (AGENTS.md "Preference changes ... must remain flexible") — the costly part (AI-task creation) is already bounded by `create_analysis_task`'s dedup + shared quota, so gating the raw save itself would only add friction with no real abuse reduction. `cv-analysis/confirm`/`review` mutate only already-existing, ownership-checked rows with no AI/external cost — abuse here has no meaningful resource/security impact, so a limiter would be an arbitrary blanket cooldown, which AGENTS.md and this task's own instructions both explicitly discourage | If usage data later shows otherwise, add a lightweight quota to confirm/review specifically — not preferences/onboarding | Verified 2026-08-22: reasoning re-confirmed against the current code; no regression test needed since no behavior changed |
| `GET /auth/callback` — rate limiting | **Implemented (session-keyed, not IP-based)** | Medium → Low | — | Was fully unprotected before this session (confirmed by a fresh full read). Now gated by `oauth_callback` (session-cookie-keyed, 20/15min, generous enough that a real one-click provider redirect or reset-link click is never affected) before `exchangeCodeForSession` is called — see the "Auth endpoints" row above for full detail | True IP/network-layer limiting (CDN/reverse-proxy) remains a "later at scale" hardening for the "many browsers, no cookie reuse" bypass case, which no app-layer session cookie can fully close | Verified 2026-08-22: `tests/db/auth-rate-limit.test.mjs`'s oauth_callback tests; exercised live via Playwright |
| Password update after recovery — rate limiting | **Implemented** | Medium → Low | — | Was fully unprotected before this session — `reset-password/page.tsx` called `supabase.auth.updateUser()` directly from the client with no server route and no gate. Now routed through `POST /api/auth/update-password` (new route), gated by `password_update` (user-id-keyed, 8/15min, generous enough that a page refresh — which never calls this endpoint on its own — can never lock a legitimate user out) | None planned | Verified 2026-08-22: `tests/db/auth-rate-limit.test.mjs`'s password_update tests; exercised live via Playwright |
| `public.rate_limit_events` grants — anon had TRUNCATE/REFERENCES/TRIGGER | **Implemented (fixed)** | Low | — | Found during the rate-limiting review: the original migration revoked `from public, authenticated` but omitted `anon`, unlike the sibling `auth_rate_limit_events` table, which correctly included it from the start. Confirmed empirically via `information_schema.table_privileges` before and after. Fixed additively: `supabase/migrations/20260822140000_harden_rate_limit_events_grants.sql` revokes all from `public, anon, authenticated`, restates the existing `service_role`-only grant. Not previously exploitable via PostgREST (TRUNCATE isn't REST-reachable, and SELECT/INSERT/UPDATE/DELETE were never granted to anon), but a genuine least-privilege inconsistency, now closed | None planned | Verified 2026-08-22: `tests/db/rate-limit-events-grants.test.mjs` (5/5) — anon and authenticated both rejected on SELECT/INSERT/UPDATE/DELETE; service_role and the trusted `replace_cv()` SECURITY DEFINER path both still work |
| Duplicate-signup raw 500 | **Implemented (fixed)** | Medium | — | **Root cause**, confirmed empirically (not assumed): `supabase.auth.signUp()` against an email that already belongs to a *confirmed* account (the normal case locally, since email confirmation is auto-enabled) returns a genuine `AuthApiError` with `status: 422, code: "user_already_exists"` — not the empty-`identities`-array 200 Supabase uses for an *unconfirmed* duplicate. `src/app/api/auth/signup/route.ts` only special-cased `status === 429` (rate limit); every other error, including this one, fell through to a generic `500`. Fixed: both duplicate paths (the 422 `user_already_exists`/`email_exists` AuthApiError, and the legacy empty-identities 200) now map to one identical 409 response — `{ error: "We couldn't create the account. If you already have an account, try signing in or resetting your password.", code: "signup_conflict" }` — never a raw Postgres/Supabase message, never a stack trace, never an affirmative "this email exists" statement. `src/app/signup/page.tsx` renders "Sign in" (`/login`) and "Reset password" (`/forgot-password`) links only when `code === "signup_conflict"`, keeps the form editable for a retry, and never blanket-converts other failure types (validation 400, rate-limit 429, generic 500) into this message. The true-unexpected-error branch now also logs `error.status`/`error.code` server-side (never `.message`, never the submitted email/password) for diagnosability, matching the pattern already used in `src/lib/payments/checkout.ts` | None planned. Full response-level indistinguishability from a brand-new signup would require an email-only "someone tried to sign up with your address" notification flow instead of any HTTP-visible difference — out of scope for this fix, noted as a residual limitation | Verified 2026-08-23: root cause reproduced empirically via a direct `signUp()` call against local GoTrue before the fix (`status: 422, code: 'user_already_exists'`); `tests/unit/signup-error-mapping.test.mjs` (new) covers the classification logic; live Playwright verification of both duplicate paths, the Sign-in/Reset-password actions, and that a genuinely new signup still succeeds normally |
| Bot/CAPTCHA protection on public forms | Not Implemented | Medium | Before deployment | `[auth.captcha]` present but disabled in `supabase/config.toml` | Enable hCaptcha or Turnstile | Verified 2026-08-21 |

### Privacy and data retention

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Account deletion path | Not Implemented (schema-level blocker below is now fixed) | High | Before deployment | No route/RPC/UI found; `audit_events.event_type` reserves `account_data_deleted` but nothing implements it. The DB-level defect that would have blocked any such endpoint (row below) is now fixed and regression-tested, but the endpoint/UI itself (user-facing "Delete my account" flow, reauthentication, explicit confirmation, cancellation/refund handling, retention policy, Storage cleanup orchestration) is still not built | Build a reviewed account-deletion endpoint that (1) calls `auth.admin.deleteUser()` first — now safe for every onboarding state — then (2) explicitly removes the user's `cvs` Storage objects as a second step (Storage is **not** cascaded by `deleteUser()` — see the Storage-lifecycle finding below), plus UI, reauthentication, and a privacy policy. **Deployment blocker per AGENTS.md §34.** | Verified 2026-08-24: schema blocker fixed and tested (see row below); endpoint/UI still absent — re-checked by `grep` across `src/` for any deletion/delete-account route, none found |
| GoTrue admin `deleteUser()` fails for any user with a `job_preferences` row | **Implemented (fixed) 2026-08-24** | High → Resolved | — | **Root cause** (unchanged from the 2026-08-24 finding below, now fixed): `public.bump_job_preferences_version_for_child()` — an `AFTER INSERT OR DELETE` trigger on `job_preference_target_roles`/`job_preference_locations` (added `20260806090070…sql:78`, `20260806090080…sql:55`) — ran `UPDATE public.job_preferences SET version = version + 1 … WHERE id = …` whenever a child join row changed. Deleting a user whose `job_preferences` row had at least one saved target role/location made Postgres cascade-delete `job_preferences` and its child join rows in one statement; the child rows' `AFTER DELETE` trigger then tried to `UPDATE` the very `job_preferences` row that same statement was already deleting, and Postgres rejected it with *"tuple to be updated was already modified by an operation triggered by the current command"* — a self-conflict, surfaced by GoTrue as an opaque 500 `AuthRetryableFetchError`. **Fix**: `supabase/migrations/20260824170000_fix_job_preferences_account_deletion_cascade.sql` drops both child triggers and the now-unused `bump_job_preferences_version_for_child()` function outright, rather than attempting a cascade-safe rewrite (an `IF EXISTS` guard would not have helped — the parent row exists throughout the same cascading statement, it is simply also being deleted; only removing the child-table UPDATE entirely closes the self-conflict). This is safe because `save_job_preferences` (`20260818090000_preferences_updated_lifecycle.sql`) already bumps `job_preferences.version` exactly once per logical save via `selection_version`, independent of any child-table trigger — the child triggers had been fully redundant with that authoritative path since 2026-08-18, and were additionally *incorrect* even outside of deletion: each individual child-row INSERT/DELETE fired its own extra version-bump UPDATE, so a single logical save that replaced several roles/locations at once bumped `.version` by far more than the intended 1 (proven and now regression-tested in `tests/db/preferences-version-exactness.test.mjs`). **On the 2026-08-22 `permission denied for table job_preferences (SQLSTATE 42501)` finding** (kept for history in prior revisions of this row): not reproduced in any of the 220 DB-suite tests run against this fix (all of which call `auth.admin.deleteUser()` via the ordinary service-role admin client) — most likely either already resolved by an earlier grants migration (`20260803090010_grant_service_role_table_access.sql`) or a misdiagnosis of this same trigger error at the time; not separately re-investigated beyond that absence of reproduction. **Storage lifecycle (Part 4 finding, new)**: `auth.admin.deleteUser()` does **not** remove the deleted user's `cvs` Storage objects — verified empirically (upload → delete → object still listed under `storage.objects`/the Storage API). A future production Delete Account endpoint must call `deleteUser()` first (now safe for every state) and then explicitly clean up that user's own Storage folder as a separate, idempotent step — never Storage-first, since a transient Storage failure before auth deletion would leave an active user with a lost CV, which is worse than a transient leftover Storage object after a successful, irreversible deletion | Fix already applied; no further action needed for this defect specifically. Recommended next step is the still-outstanding account-deletion endpoint/UI (row above), which must include the Storage cleanup step described here | Verified 2026-08-24: reproduced the original defect's exact failure mode was already on record from 2026-08-24's earlier finding; **this fix verified by**: `tests/db/account-deletion-cascade.test.mjs` (11/11 — calls `auth.admin.deleteUser()` directly, with **no** child-row pre-deletion, across new-user/CV-only/custom-prefs/one-role/multi-role/one-location/multi-location+complete-onboarding/replaced-CV+pending-task/completed-analysis+feedback/full-downstream-graph/two-isolated-users scenarios) and `tests/db/preferences-version-exactness.test.mjs` (5/5 — proves exactly-once version increments post-fix); full `npm run test:db` 220/220, `npm test` (unit+workflow+db) all green, `npx tsc --noEmit` clean, `npm run lint` 0 errors, `npm run build` succeeds; Batoul's real local account (CV, preferences, subscription, task, analysis, Storage object) confirmed byte-for-byte unchanged (per-table SHA-256 fingerprint, before vs. after) across the entire destructive fixture-testing pass; zero `db-test-*` fixture users/rows/Storage objects remained afterward |
| Privacy policy published | Not Implemented | High | Before deployment | No `/privacy` route under `src/app` | Publish a privacy policy before any public/EU-facing launch. **Deployment blocker per AGENTS.md §34.** | Verified 2026-08-21 |

### Email and OAuth

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Production SMTP configured | Cannot Verify | Medium | Before deployment | Dashboard-side config, outside this repo | Confirm in Supabase dashboard → Auth → SMTP before launch | Not verifiable from this environment |
| OAuth redirect validation | Implemented | Low | — | `src/app/auth/callback/route.ts`; `src/lib/safeRedirect.ts` | Add a regression test to `tests/unit` | Verified 2026-08-21 |

### Payments

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| No fake payment success possible | Implemented | Low | — | `src/lib/payments/whish/provider.ts` throws `WhishNotConfiguredError` on every real operation | None until real Whish credentials/docs exist | Verified 2026-08-21 |
| Early renewal (verified before `current_period_end`) never interrupts or prematurely overwrites the active period | **Implemented (fix) 2026-09-03** | High → Resolved | — | **Found during a second dedicated read-only re-verification pass**, immediately after the period-boundary fix in the row below (which was itself still unconditional): `mark_payment_verified`'s renewal branch computed `v_period_start := current_period_end; v_period_end := v_period_start + 1 month` and wrote it straight onto `subscriptions.current_period_start/end` **regardless of whether `current_period_end` was still in the future**. Example: an active period Sept 1–Oct 1, a renewal verified Sept 20 — the fix from the row below already got the renewed window right (Oct 1–Nov 1), but activated it **immediately on Sept 20**, 11 days early, discarding the still-active Sept 1–Oct 1 period from the row. No entitlement check in this codebase actually requires `current_period_start <= now()` (only `current_period_end > now()`, confirmed by exhaustively grepping every use of both columns across all 74 migrations and `src/`), so `plan_eligible` itself never flipped false — but the model was still silently replacing active-period state 11 days prematurely, which the task's own invariant list explicitly forbids regardless of whether today's specific checks happen to survive it. **Fix**: `subscriptions` gains `next_period_start`/`next_period_end`/`next_price_version_id` (nullable, `next_period_start < next_period_end` enforced). `mark_payment_verified`'s renewal branch now branches on `current_period_end > now()`: EARLY → `current_period_start/end` are left completely untouched; the paid period is queued onto `next_period_*` instead, chained onto any already-queued `next_period_end` (so a second early renewal extends the queue rather than colliding with it) — `activate_subscription` is deliberately not called at all on this path. ON-TIME/LATE → unchanged behavior from the row below (activates immediately). A new `promote_due_subscription_period(user_id)` (idempotent, no-op when nothing is due) physically promotes a queued period into `current_period_*` once `current_period_end` has actually passed; called opportunistically from `create_payment_attempt` (before it reads the subscription for any decision) and from `mark_payment_verified` itself (before judging "is this early") — no cron/background-job infrastructure was added, consistent with "do not build a large billing subsystem." Because `get_onboarding_readiness` is `STABLE` (cannot write), it independently mirrors the identical "is a queued period actually due" condition read-only, computing an `effective_period_end` for the `plan_eligible` check — this closes the gap between the moment `current_period_end` elapses and the next payment-related RPC call, where a plain dashboard read would otherwise see the stale row. A `pg_advisory_xact_lock` per user in `mark_payment_verified` additionally serializes concurrent verifications of two *different* attempts for the same user, so queued-period chaining can never race (the existing row-level `FOR UPDATE` only serializes two calls for the *same* attempt id). Chose the "defer" architecture (option 2 of the two the task offered) over rejecting early finalization outright (option 1): rejecting after a real provider payment already cleared would leave captured money with no recorded entitlement, and this product's renewal path is user-initiated (a user can legitimately want to renew early to lock in the current price version before an announced increase), not provider-scheduled | None — both the physical (write-side) and virtual (read-side) halves are in place; a future automated-billing integration renews through the exact same `create_payment_attempt`/`mark_payment_verified` path unchanged | Verified 2026-09-03: `tests/db/price-versioning-and-upgrade.test.mjs`, new describe block `"Early renewal: deferred period, uninterrupted access, correct promotion"` (6 tests) proves — an early renewal leaves `current_period_start/end` byte-for-byte untouched and queues the exact Oct 1→Nov 1 boundary from the worked example; `get_onboarding_readiness.plan_eligible` stays `true` immediately before and after the early renewal (uninterrupted access); a duplicate callback for the same attempt is a true no-op (queued period not re-extended, provider reference unchanged); two concurrently-verified early-renewal attempts for the same user chain into two full consecutive months (`old_end→+1mo→+2mo`), never both landing on the same window, and their own `payment_attempts.period_start/end` snapshots are back-to-back; a renewal verified exactly at/after the boundary still activates immediately with no deferral; `promote_due_subscription_period` physically promotes a due queue (verified both via a direct read reporting correctly pre-promotion and via `create_payment_attempt` triggering the promotion and the row reflecting it afterward). Also updated the pre-existing renewal-period test (previously exercised with 10 days of the old period still remaining — actually the early case, now behaving differently by design) to genuinely exercise the on-time/late path, and added an explicit assertion there that `next_period_*` stays null for that path. Full re-run: `npm run test:db` 284/284 (74 suites, up from 278/73 — zero regressions), `npm run test:unit` 283/283, `npm run test:workflow` 127/127, `npx tsc --noEmit` clean, `npm run lint` 0 errors, `npm run build` succeeds (see dated confirmation) |
| Renewal/upgrade billing-period boundaries are server-computed, not caller-trusted; `provider_payment_id` is unique | **Implemented (fix) 2026-09-03** | High → Resolved | — | **Found during a dedicated read-only re-verification pass** (before committing the price-versioning work in the row below): `mark_payment_verified(p_payment_attempt_id, p_provider_payment_id, p_period_start, p_period_end)` took `p_period_start`/`p_period_end` **entirely from the caller**, with zero server-side validation against the subscription's existing period, for every `purchase_type` including `renewal` and `upgrade`. Nothing in the database enforced "a renewal begins exactly at the old `current_period_end`" or "an upgrade preserves the original `current_period_end`" — both were just hoped-for behavior in whatever code eventually calls this service-role-only RPC (none exists yet — Whish is unconfigured). A future webhook/admin-action bug (e.g. passing `now()` for a renewal, or a fresh date for an upgrade) would have silently reset, gapped, overlapped, or extended a paid period, with no constraint to catch it. Separately, `payment_attempts.provider_payment_id` (column present since `20260802090020_create_payment_attempts.sql`, the very first payments migration) had **no uniqueness constraint at all** — two different `payment_attempts` rows could both be marked `'paid'` against the identical real-world provider reference. **Fix** (in `supabase/migrations/20260903090000_add_price_versioning_and_upgrade_locking.sql`, edited in place — not yet committed/shared at the time of this fix, so corrected directly rather than layered with a third migration): `mark_payment_verified` now derives the period **server-side from `purchase_type`**, ignoring the caller's dates outright for `upgrade` (preserves `subscriptions.current_period_start/end` exactly) and `renewal` (`current_period_end` → `current_period_end + 1 month`, using the attempt's own locked `billing_period`); `p_period_start`/`p_period_end` are used only for `initial` (no prior period exists to derive from — a real provider confirmation is the only source of truth there). Both paths raise a safe `PT500` if the subscription has no period to preserve/renew from, rather than inventing one. Added `payment_attempts_provider_payment_id_unique unique (provider, provider_payment_id)` (NULLs remain unconstrained per standard SQL multi-column-unique semantics, so unverified attempts are unaffected) | None — both invariants are now database-enforced, independent of caller correctness | Verified 2026-09-03: `tests/db/price-versioning-and-upgrade.test.mjs` — new describe blocks prove (all tests deliberately pass WRONG caller-supplied dates to prove the DB, not caller discipline, is what's authoritative): a renewal starts exactly at the old `current_period_end` and runs one `billing_period`, never the caller's date; an upgrade preserves the original `current_period_start`/`current_period_end` exactly; an upgrade with no existing period fails safely; an initial purchase still uses the caller-supplied period (bootstrap case, correctly unaffected); two different attempts cannot both be verified with the same `provider_payment_id` (unique-violation rejected, the second attempt's `status` stays `'created'`); multiple never-verified (`provider_payment_id IS NULL`) attempts coexist without issue; an authenticated user cannot `UPDATE` their own `payment_attempts` row (`amount`, `status`, `price_version_id`, `purchase_type` all rejected, verified unchanged via admin re-read) or their own `subscriptions` row (`plan_code`, `status` both rejected). Full re-run: `npm run test:db` 278/278 (73 suites, up from 270/70 — zero regressions in any pre-existing test), `npm run test:unit` 283/283, `npm run test:workflow` 127/127, `npx tsc --noEmit` clean, `npm run lint` 0 errors, `npm run build` succeeds (see dated confirmation) |
| Explicit price versioning; Student-to-Pro upgrade locked to the customer's own paid price schedule | **Implemented (fix) 2026-09-03** | High → Resolved | — | **Root cause found during the plan-aware-preferences/pricing hardening pass**: `create_payment_attempt()` as it stood after `20260902090020_add_student_to_pro_upgrade_support.sql` computed an in-period Student-to-Pro upgrade as `(public.plans.price_amount for 'pro' — read LIVE at request time) − (the Student's actual paid amount)`. `public.plans` has always held only the CURRENT price, with no historical schedule — so a public Pro price change between a Student's purchase and their upgrade would silently charge `(new Pro price − old Student credit)` instead of two prices from the same original schedule (e.g. Student $9/Pro $18 at purchase, public price later moves to Student $15/Pro $25 → the pre-fix code would have charged $25−$9=$16 instead of the correct $18−$9=$9). No test exercised a price change between purchase and upgrade, so this was undetected. **Fix**: `supabase/migrations/20260903090000_add_price_versioning_and_upgrade_locking.sql` adds `price_versions`/`plan_prices` (an explicit, RLS-read-only, service-role-write-only locked price schedule per version) plus `payment_attempts.price_version_id`/`purchase_type`/`source_plan_code`/`billing_period`/`period_start`/`period_end` and `subscriptions.price_version_id`, backfilled from the one schedule that has ever existed. `quote_student_to_pro_upgrade()` (new, internal-only — no `authenticated` grant, would otherwise leak another user's upgrade eligibility/credit) now sources BOTH the Student credit and the destination Pro price from `subscriptions.price_version_id` (the customer's own locked schedule), never the live active version; `create_payment_attempt()` also now rejects an active Pro customer purchasing Student outright (no downgrade path existed as a guard before this fix — nothing previously stopped an already-Pro user from creating a Student `payment_attempts` row, though nothing would have activated it as a downgrade since `activate_subscription` simply trusts whatever plan the verified attempt names) and classifies every attempt's `purchase_type` (`initial`/`renewal`/`upgrade`) for an immutable audit trail. `plans.price_amount`/`currency` are intentionally left in place as the current-display-only value (`Pricing.tsx` marketing-copy contract), documented as no longer the checkout-authoritative source | None — price versioning is now the durable pattern; a future price change is always a new `price_versions`/`plan_prices` row (service-role/migration only), never an edit to an existing one | Verified 2026-09-03: `tests/db/price-versioning-and-upgrade.test.mjs` (new, 17/17) proves — fresh purchases price from the active version; an already-active same-plan purchase is billed as a renewal from the CURRENT active version; an in-period upgrade is locked to the customer's own version both before and after a public price change (explicit regression test reproducing the exact pre-fix bug numbers); a customer on a newer locked version upgrades using that version's prices; the upgrade amount floors at 0; a locked version missing a compatible Pro price fails safely with zero attempts created; an active Pro user cannot create a Student attempt; an already-Pro "upgrade" request bills as an ordinary renewal; an expired Student gets no credit; a verified purchase's amount/version/period never change after a later public price change; `mark_payment_verified` is idempotent under a duplicate callback with a different provider reference; `price_versions`/`plan_prices` are `authenticated`-select-only (anon has no grant at all, matching `public.plans`) with insert/update/delete rejected for `authenticated`; `payment_attempts` stay cross-user-private; unknown/forged RPC arguments (a client attempting to pass a price or credit) are rejected outright. Full suite re-run: `npm run test:db` 265/265 (69 suites, includes the above plus every pre-existing DB test unaffected), `npm run test:unit` 283/283, `npm run test:workflow` 127/127, `npx tsc --noEmit` clean, `npm run lint` 0 errors (11 pre-existing warnings, none in files touched by this change), `npm run build` — see the dated confirmation this same day |

### n8n and AI automation

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Credentials referenced by name only, never hardcoded | Implemented | Low | — | `n8n-workflows/cv-analysis-worker.ts` — `newCredential(...)` references only | None | Verified 2026-08-21 |
| Idempotent worker retries | Implemented | Low | — | `Prefer: resolution=ignore-duplicates`; `FOR UPDATE SKIP LOCKED` in `claim_analysis_task` | None | Verified 2026-08-21 |
| Live workflow changes require explicit approval | Partially Implemented | Low | — | Documented in AGENTS.md §34; no automated/technical enforcement exists (e.g., no CI gate or access control blocking a `publish_workflow`/import action) | Continue requiring sign-off before any `publish_workflow`/import to the running instance; consider a technical control (e.g., restricted n8n publish credentials) if the process rule alone proves insufficient | Verified 2026-08-21 — documentation only, not a technical control |

### CI/CD

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Tests run in CI | Partially Implemented | Medium | Before starting Automation 2 | `.github/workflows/ci.yml` — unit + workflow wired this phase; DB tests intentionally deferred (see above) | Land the DB-in-CI job once proven reliable | Verified 2026-08-21 |
| Branch protection on `main` | Cannot Verify | Medium | Before starting Automation 2 | No `gh` CLI/API access from this environment | Confirm required-checks list includes `quality-checks` (and later `db-tests`) via GitHub repo settings | Not verifiable from this environment |

### Testing

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Full suite wired to one command | Implemented | Low | — | `package.json` `test`/`test:unit`/`test:workflow`/`test:db` scripts | Add new test files to the relevant existing directory — no script changes needed | Verified 2026-08-24: `npm test` run locally, 483/483 passing (see the matching row at the top of this document for the full breakdown) |
| Previously-reported flaky DB test ("failed once, passed on rerun") | **Cannot Reproduce** | Low | — | No specific failing test, seed, or error output was available to investigate from — the report was of a past, unlogged occurrence. Re-ran `npm run test:db` 3 times in full (203/203, 203/203, 203/203) and, since intermittent failures are most plausible in code that exercises real concurrency/timing (`Promise.all` of N simultaneous RPC calls racing a rolling-window rate limit), ran the three concurrency-heavy files in isolation 3 additional times (`tests/db/rate-limits.test.mjs` + `tests/db/auth-rate-limit.test.mjs` + `tests/db/payment-attempt-rate-limit.test.mjs`: 62/62 each of 3 runs). All 6 runs were clean. Per AGENTS.md §10 ("do not guess", "do not apply random changes until something appears to work"), no speculative fix or added retry/wait was introduced — there is nothing here to fix without a reproduction | If this recurs, capture the exact failing test name, the full `node --test` output, and whether the local Supabase stack had just been freshly started (a cold-start health-check race is the next most likely candidate, distinct from the concurrency hypothesis already ruled out) | Investigated 2026-08-24: 6 consecutive clean runs, 0 failures observed |
| DB-test fixture isolation from manual development data | **Implemented** | High → Low | — | **Root cause** (2026-08-24, prompted by manual dev data appearing to "disappear" after a test run): `tests/db/*.test.mjs` run against the same local Supabase project manual development uses — there is no separate test database. The old `deleteTestUsers()` only `console.error`'d a failed `auth.admin.deleteUser()` and moved on; combined with the `job_preferences`-trigger self-conflict documented in the row above (which made deletion fail deterministically for any fixture user who saved preferences with a reference role/location), this silently accumulated **134 orphaned fixture auth users** (with their cascaded `profiles`/`cvs`/`job_preferences`/etc.) in the shared local database over the life of this branch — never Batoul's real account, which stayed intact throughout (verified byte-for-byte, before/after, at every step below), but a real contamination risk and a false signal that manual data was being deleted. **Fix**: new `tests/db/localTestGuard.mjs` (hostname allowlist + `LOCAL_TEST_DB_MARKER` + canonical-catalog check — three independent guards, never hostname alone) and `tests/db/fixtureCleanup.mjs` (retry-with-backoff, delete, and **verify**, throwing rather than logging on failure; includes the `job_preferences`-children-first workaround for the trigger bug above). `helpers.mjs` now tracks every created fixture id in a per-process manifest with its own top-level `after()` safety net, closing the gap where a partial `Promise.all` failure in a file's `before()` could leave a user untracked. `scripts/run-db-tests.mjs` orchestrates `test:db` with `--test-concurrency=1` and a post-run verification sweep; `scripts/db-test-crash-recovery-sweep.mjs` provides a marker-scoped (`^db-test-[a-z0-9+-]+@test\.local$` — the `+` allows the plus-tagged fixture form `tests/db/auth-credential-policy.test.mjs` uses to prove plus-addressing, without loosening the required `db-test-` prefix or `@test.local` domain), age-gated, dry-run-by-default inventory/cleanup tool for fixtures abandoned by a crashed run (deletion requires both `--apply` and `CONFIRM_CRASH_SWEEP=yes`) | None planned — keep new `tests/db/*.test.mjs` files using `createTestUser`/`uploadFakeCv`/etc. from `helpers.mjs` as-is; the manifest/safety-net/retry logic applies automatically | Verified 2026-08-24: focused run (3 files, 38/38) then full `test:db` (204/204) both left **zero** new fixtures behind (confirmed by direct query, not just the runner's own claim); the pre-existing 134-fixture backlog (redacted inventory saved) plus 3 additional ambiguous manual/e2e QA accounts and 1 orphaned Storage object were reviewed and removed in a separate, explicitly-approved cleanup pass — Batoul's auth identity/profile/subscription/active CV/Storage object/preferences/role+location joins/analysis task/CV analysis confirmed byte-for-byte unchanged before vs. after (deep JSON diff of a full per-table fingerprint), and her `get_onboarding_readiness()` decision was independently recomputed read-only (never signing in as her) confirming `next_step = dashboard`, both before and after. Final state: 1 auth user total (Batoul), every fixture table back to exactly her own row count |

### Playwright E2E verification

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Playwright MCP tool availability | **Available** | Low | — | The Playwright MCP browser tools (`browser_navigate`, `browser_snapshot`, `browser_click`, etc.) load and are callable in this session — confirmed via a schema-only lookup, no browser launched | None | Verified 2026-08-24 |
| Open-redirect fix (`safeRedirect.ts` control-character guard) — live browser verification | **Implemented, browser-verified** | Low | — | Ran the local dev server (`npm run dev`) against an isolated fixture user (`db-test-e2e-review-*@test.local`, created with a known password, active CV + complete preferences — a "returning completed user") and drove real browser navigations via Playwright MCP, not just the unit tests in `tests/unit/safe-redirect.test.mjs` | None | Verified 2026-08-24, this pre-commit review pass: (1) login with the fixture's real credentials landed on `/dashboard` directly, matching `next_step: "dashboard"` from `get_onboarding_readiness()`; (2) `/login?next=%2Fdashboard%3Ftab%3Dapplications` (safe internal path + query) while authenticated redirected to exactly `/dashboard?tab=applications`, same origin; (3) `/login?next=%2Freset-password` redirected to exactly `/reset-password`, same origin; (4) `/login?next=%2F%09%2Fevil.com` (the encoded-tab payload that previously bypassed the guard) redirected to `/dashboard`, **not** `evil.com` — the fix holds against a real browser's URL resolution, not just Node's; (5) `/login?next=%2F%2Fevil.com` (protocol-relative) and `/login?next=https%3A%2F%2Fevil.com` (absolute external) both also redirected to `/dashboard`, never left `localhost:3000`; (6) zero console errors across the entire session (`browser_console_messages`, all levels, full session). Fixture deleted afterward via `deleteFixtureUsers()` (retry+verify); independently reconfirmed zero fixtures remain and Batoul's own row counts (1 profile/cv/job_preferences/subscription/analysis_task/cv_analysis/storage object) are unchanged before vs. after. Did not attempt a real Google OAuth flow (no credentials automated, per instruction) or the `/auth/callback` route directly (requires a real, single-use provider/recovery code this review has no way to generate safely) — that route shares the exact same `isSafeRedirectPath()` function proven above and is additionally covered by `tests/unit/safe-redirect.test.mjs`'s dedicated "as they arrive via `searchParams.get()`" cases, but has not itself been driven through a live OAuth exchange |
| Broader browser E2E coverage of this branch (every other routing/auth row above that references "live Playwright verification") | **Not re-verified in this pass** | Low | Before this branch is treated as fully, exhaustively browser-verified | Several rows elsewhere in this document describe Playwright passes run at earlier points in this branch's history (2026-08-22 rate-limiting phase, 2026-08-24 auth-UX/routing phase) — those are accurate records of what was checked *at that time*, kept as-is rather than rewritten. This pass only re-verified the specific fix made during pre-commit review (the row above) plus the `auth-credential-policy.test.mjs` cleanup fix (verified via `node --test`, not a browser — it has no UI surface) | If a full re-run is wanted, repeat the earlier phases' scenarios (Google OAuth landing page, signup duplicate-account UX, password show/hide, rate-limit 429 UI) against this branch's exact final diff | Not re-run this pass — treat earlier-phase Playwright rows as historical, not as current-diff verification |



| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Error tracking / alerting service | Not Implemented | High | Before deployment | Only `console.error`, no external sink | Wire existing safe log call sites into an error-tracking service | Verified 2026-08-21 |

### Backups and disaster recovery

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Point-in-time recovery enabled and restore tested | Cannot Verify | High | Before deployment | Dashboard-side, no repo evidence | Confirm in Supabase dashboard → Database → Backups; run one restore drill | Not verifiable from this environment |

### Performance and scalability

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Load-tested worker throughput | Cannot Verify | Medium | Later at scale | No load-test evidence exists | Run k6/Artillery against a staging environment once traffic is real | Not verifiable from this environment |

### Deployment and rollback

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Documented migration rollback procedure | Not Implemented | Medium | Before deployment | Forward-only migrations; no down-migration scripts | Document a rollback/forward-fix procedure per AGENTS.md §33 | Verified 2026-08-21 |

### Documentation

| Finding | Current status | Severity | Required timing | Concrete evidence | Recommended remediation | Verification status |
|---|---|---|---|---|---|---|
| Incident-response runbook | Not Implemented | Medium | Before deployment | No runbook file found | Write one before public launch | Verified 2026-08-21 |

### n8n automation workflows (Source Intelligence Analyzer, Job Ingestion Pilot Orchestrator)

Findings from the 2026-09-19 final pre-database-migration audit. No structural blockers found in either workflow — see `docs/n8n-workflow-change-process.md` for the deployment process itself. Also see AGENTS.md §34: n8n changes require the repo→validate→diff→live→re-read sequence, full-replacement node updates only (never partial JSON-pointer paths into arrays — see the 2026-09-19 incident documented in `docs/n8n-workflow-change-process.md`).

**A. Required environment promotion tasks** (before production activation of either workflow):
- Change the centralized `supabaseBaseUrl` in each workflow's `Workflow Configuration` node from the local Docker URL to the production Supabase URL. Confirmed only-working pattern on this n8n Community instance — `$env` is blocked (`N8N_BLOCK_ENV_ACCESS_IN_NODE`), n8n Variables requires an unavailable license tier, and `{{ $credentials.host }}` is not accessible from a generic `httpRequest` node's own expressions. All three confirmed empirically 2026-09-19.
- Change `environment` from `'local'` to the appropriate production value (informational only — not a safety gate, does not affect behavior).
- Configure and bind the correct production `Supabase Service Role` credential (same name, new environment-specific values) to every node that needs it.
- Verify no production workflow still references `localhost`, `host.docker.internal`, `127.0.0.1`, local ports, or development-only services.
- Explicitly review `mode`, schedule configuration, and `active` state before production activation — do not assume today's local values are what production should start with.
- Perform a controlled production smoke test (one manual execution, dry-run or bounded write) before enabling normal scheduled execution.

**B. Required before expanding Job Ingestion to new providers:**
- Review/widen the `jobs.source_type` database check constraint before ingesting providers beyond the currently-supported `greenhouse`/`lever`/`workable` values (e.g. `oracle`, which Source Intelligence Analyzer can already detect but Job Ingestion cannot yet write).

**C. Pre-launch hardening / scale items** (not blockers for continuing development — explicit reminders for later):
- Evaluate a DB-level uniqueness constraint on `source_intelligence.source_id` as defense-in-depth against concurrent Source Intelligence executions (currently correctness relies entirely on the selection RPC's `NOT EXISTS`, not a schema constraint).
- Add/verify protection against overlapping Source Intelligence executions before increasing run frequency or introducing concurrency (no overlap guard exists today).
- Review pagination for ATS job boards before Registry-driven ingestion expands to sources with very large job counts (none of the 3 current adapters paginate).
- Review the Source Intelligence RPC/index/query performance as `source_intelligence` history grows significantly.
- Review execution-summary/result array sizes (`Build Run Summary`, `Build Execution Summary`) before batches grow to hundreds of sources per execution.
- Add a lightweight production pre-flight/config sanity check so a workflow cannot accidentally be activated while still pointing at local infrastructure.
- Verify production failure/recovery behavior for Supabase outages, provider 429/5xx responses, n8n restarts, and partial batch failures — today's behavior was audited and is reasonable at pilot scale (graceful per-item failure isolation in both workflows, real backoff in Job Ingestion) but has not been tested against a real production outage.

---

*Last full audit: 2026-08-21 (web app). n8n automation-layer audit: 2026-09-19 — see the section above. This file supersedes any severity/timing labels stated in prior chat-only reports — if the two ever disagree, this file is correct.*
